apiVersion: v1
metadata:
  # Department AskID
  askId: UHGWM110-017197
  # TODO: replace with the workspace ID from your rally project URL. example: https://rally1.rallydev.com/#/61545860298ud/iterationstatus
  caAgileId: 61545860298ud
  # Must match your applications <groupId>:<artifactId>
  projectKey: com.optum.riptide.devops:jenkins-scripts
  # Must match your application's <artifactId>
  projectFriendlyName: jenkins-scripts
  # No change required
  componentType: 'code'
  # No change required until your application passes higher gates. See https://sonar.optum.com/quality_gates for available values.
  targetQG: 'ADOPTION'
